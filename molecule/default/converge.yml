---
- name: Converge
  hosts: all
  become: yes
  vars:
    rke2_version: v1.27.1+rke2r1
    rke2_snapshotter: native
    rke2_config_dir_mode: "0750"
  roles:
    - role: "{{ lookup('env', 'MOLECULE_PROJECT_DIRECTORY') | basename }}"
